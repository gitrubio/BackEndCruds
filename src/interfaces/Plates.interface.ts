export interface IPlate {
    id : number
    name: string
    price : number
    region : string
}

export interface ITaks {
  id: number;
  name: number;
  priority: number;
  horaInicio: Date;
  horaFin: Date;
}